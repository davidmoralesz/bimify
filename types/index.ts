type Settings = {
  maxWidth: number
  maxHeight: number
  quality: number
  suffix: string
  useWebP: boolean
  maxFiles: number
}

type Tips = {
  maxDimension: string
  maxWidth: string
  maxHeight: string
  quality: string
  suffix: string
  useWebP: string
}
